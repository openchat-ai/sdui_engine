name: sdui_engine
description: Server-driven UI for Flutter. Render widgets from JSON — no app store submission needed.
version: 1.0.0
repository: https://github.com/openchat-ai/sdui_engine
homepage: https://github.com/openchat-ai/sdui_engine
topics: [flutter, sdui, server-driven-ui, json-ui]

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
